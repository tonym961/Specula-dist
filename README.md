# Specula — distribution

Public download channel for **Specula** (the source lives in a private repo). Hosts the signed
debug APK releases and the `update.json` manifest the app's in-app auto-updater reads.

- Latest APK: see **Releases**.
- Auto-update manifest: [`update.json`](update.json).
